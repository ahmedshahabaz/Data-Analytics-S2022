*** All the data should be in the following order:

	ImageDataSet/train/   => Train data
	ImageDataSet/test/    => Test data

*** run the python script called "test_model.py" with the following command: "python3 tes_model.py --batch_size=32" which will show the accuracy on the test set.

*** The "test_model.py" will also generate the CSV file of model prediction.

*** run the python script called "train.py" to train a model.

*** run the "non_dl.py" script to see the performance of the non_dl methods.
